// Generated by CoffeeScript 1.10.0
(function() {
  var ActionGenerator, BuiltInActionBridge, CustomActionBridge,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  BuiltInActionBridge = require('./built-in-action-bridge');

  ActionGenerator = require('../actions/action-generator');

  CustomActionBridge = (function(superClass) {
    extend(CustomActionBridge, superClass);


    /**
     * @protected
     * @type {Object.<Event>
     */

    CustomActionBridge.prototype._actions = null;


    /**
     * @override
     */

    function CustomActionBridge(parent, config, index) {
      var actionConfig, actionGenerator, i, j, len, ref;
      CustomActionBridge.__super__.constructor.call(this, parent, config, index);
      this._actions = {};
      i = 0;
      if (config.actions) {
        actionGenerator = new ActionGenerator(this);
        this.checkConfig(config.actions, 'config.actions', 'array');
        this.log('debug', "Construct event objects was started.");
        ref = config.actions;
        for (j = 0, len = ref.length; j < len; j++) {
          actionConfig = ref[j];
          this._actions[actionConfig.name] = actionGenerator.generate(this, actionConfig, i);
          this.on(actionConfig.name, this._actions[actionConfig.name].run);
        }
        this.log('debug', "Construct event objects was finished.");
      }
    }

    return CustomActionBridge;

  })(BuiltInActionBridge);

  module.exports = CustomActionBridge;

}).call(this);

//# sourceMappingURL=custom-action-bridge.js.map
