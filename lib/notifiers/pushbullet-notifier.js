// Generated by CoffeeScript 1.10.0
(function() {
  var Notifier, Pushbullet, PushbulletNotifier,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty,
    slice = [].slice;

  Pushbullet = require('pushbullet');

  Notifier = require('./notifier');

  PushbulletNotifier = (function(superClass) {
    extend(PushbulletNotifier, superClass);


    /**
     * @protected
     * @type Pushbullet
     */

    PushbulletNotifier.prototype._pushbullet = null;


    /**
     * @override
     */

    function PushbulletNotifier(parent, config, index) {
      this._onNotify = bind(this._onNotify, this);
      var apiKey;
      PushbulletNotifier.__super__.constructor.call(this, parent, config, index);
      apiKey = this._checkConfig(config, 'apiKey', 'string');
      this._pushbullet = new Pushbullet(config.apiKey);
      this._pushbullet.me((function(_this) {
        return function(err, response) {
          if (err) {
            _this.log('fatal', 'Pushbullet auth failed.');
            return process.exit(1);
          } else {
            return _this.log('info', "Pushbullet auth succeed. response=" + (JSON.stringify(response)));
          }
        };
      })(this));
    }


    /**
     * @override
     */

    PushbulletNotifier.prototype._onNotify = function() {
      var args, message, options;
      options = arguments[0], args = 2 <= arguments.length ? slice.call(arguments, 1) : [];
      message = this._makeMessage.apply(this, [options].concat(slice.call(args)));
      return this._pushbullet.note(null, message, message, (function(_this) {
        return function(err) {};
      })(this));
    };

    return PushbulletNotifier;

  })(Notifier);

  module.exports = PushbulletNotifier;

}).call(this);

//# sourceMappingURL=pushbullet-notifier.js.map
