extends ./base-entity-component.jade

block variables
  -
    var isServer = true;

block body
  +children("board-component", "board", "boards", "tab")
  +children("notifier-component", "notifier", "notifiers", "box")
  +children("job-component", "job", "jobs", "box")
