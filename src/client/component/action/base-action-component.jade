include ../base-entity-component-mixin.jade

block variables

section.action(v-if="!add")
  .header
    +dropdown()
    h1 #[i.fa(class="fa-#{icon}")] #{singularTitleizeName} Action: {{entity.name}}
  .body
    block body
button.btn.btn-primary(v-else, type="button", @click="showModal = true") #[i.fa(class="fa-#{icon}")] #{singularTitleizeName} Action
+modal(true)
